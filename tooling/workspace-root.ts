/**
 * Workspace root placeholder so `pnpm typecheck` has a strict TypeScript project
 * before apps/server/packages are added in later Epic 00 milestones.
 */
export const workspaceRoot = 'project-genesis' as const;
